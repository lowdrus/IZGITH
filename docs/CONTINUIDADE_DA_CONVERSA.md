# Continuidade da conversa IZGITH

Este arquivo registra a continuidade oficial entre a conversa do projeto e o repositório.

Escopo consolidado: v4.1 FULL ONE, v4.2 SMART SYSTEM, v4.3 SECURE LAB, arquitetura offline-first com sincronização opcional, popup, dashboard, drag and drop, fila de múltiplos pacotes, 36 temas, efeitos 3D, compliance, monitor de GitHub, quarentena, rollback planejado, sandbox e perfis de execução.

O código ativo permanece em `extension/` e `host/`. Fontes históricas ficam em `archive/legacy/` até revisão técnica individual antes de integração ao produto distribuível.
