const $=id=>document.getElementById(id),$$=s=>Array.from(document.querySelectorAll(s));
const TITLES={overview:'PAINEL GERAL',tools:'Ferramentas',settings:'Configurações',queue:'Logs',themes:'Temas',eula:'EULA',guide:'Guia Rápido'};
const THEMES=Array.from({length:36},(_,i)=>{const family=['cyber','premium','matrix','glass'][Math.floor(i/9)];return [`${family}-${String(i%9+1).padStart(2,'0')}`,`hsl(${i*10} 30% 7%)`,`hsl(${i*10} 30% 12% / .85)`,`hsl(${i*10} 90% 65%)`,`hsl(${(i*10+45)%360} 85% 65%)`]});
const PROVIDER_LABELS=['Grok','ChatGPT','Claude','Gemini','Copilot','Perplexity','DeepSeek','Poe','Le Chat','You.com','Meta AI','Qwen','HuggingChat','Character.AI'];
function openTab(id){$$('.tab').forEach(e=>e.classList.toggle('active',e.id===id));$$('.nav').forEach(e=>e.classList.toggle('active',e.dataset.tab===id));$('pageTitle').textContent=TITLES[id]||'IZGITH';location.hash=id}
$$('.nav').forEach(b=>b.addEventListener('click',()=>openTab(b.dataset.tab)));
function applyTheme(name){const t=THEMES.find(x=>x[0]===name)||THEMES[0],r=document.documentElement;r.dataset.theme=t[0];r.style.setProperty('--bg',t[1]);r.style.setProperty('--panel',t[2]);r.style.setProperty('--a',t[3]);r.style.setProperty('--b',t[4]);chrome.storage.local.set({theme:t[0]});$$('.theme').forEach(e=>e.classList.toggle('active',e.dataset.theme===t[0]))}
function buildThemeGrid(){const g=$('themeGrid');g.replaceChildren(...THEMES.map(t=>{const b=document.createElement('button');b.className='theme';b.dataset.theme=t[0];b.textContent=t[0].replace(/-/g,' ');b.style.setProperty('--sw1',t[3]);b.style.setProperty('--sw2',t[4]);b.onclick=()=>applyTheme(t[0]);return b}))}
async function renderQueue(){const d=await chrome.storage.local.get({izgithQueue:[]}),q=d.izgithQueue||[];$('statQueue').textContent=q.length;$('dashboardQueue').textContent=q.length?q.map(x=>`${x.name||'item'} · ${x.size||0} B`).join('\n'):'Fila vazia.'}
function addMessage(who,text){const row=document.createElement('div');row.className='chat-message';row.textContent=`${who}: ${text}`;$('izartMessages').appendChild(row);$('izartMessages').scrollTop=$('izartMessages').scrollHeight}
function reply(name,text){const s=text.toLowerCase();if(s.includes('conv-d')||s.includes('conv d'))return'CONV-D coloca Baixar Conversa nas páginas suportadas e permite Tudo ou Ultima Rodada, com escolha de formato e local de salvamento.';if(s.includes('sonpef'))return'SONPEF unifica os scripts selecionados diretamente no navegador, sem Native Messaging.';if(s.includes('tema')||s.includes('2d')||s.includes('3d')||s.includes('4d'))return'Os 36 temas funcionam com profundidade 2D, 3D e 4D.';if(name==='Ayella')return'Olá! Sou Ayella. Posso orientar o uso do IZGITH, configurações e exportação de conversas.';if(name==='Júlia')return'Olá! Sou Júlia. Posso ajudar com o hub de assistentes e o fluxo do IZGITH.';return'Olá! Sou IZART. Posso ajudar com diagnóstico, CONV-D, SONPEF e as ferramentas do IZGITH.'}
let currentAssistant='IZART';
function openAssistant(name){currentAssistant=name;const chat=$('izartChat');chat.hidden=false;$('assistantChatTitle').textContent=name;$('izartMessages').replaceChildren();addMessage(name,'Olá! Sou '+name+'. Como posso ajudar?');chat.scrollIntoView({behavior:'smooth',block:'nearest'});$('izartInput').focus()}
function setupAssistantArea(){const overview=$('overview'),grid=overview&&overview.querySelector('.assistant-grid'),chat=$('izartChat');if(grid&&chat&&chat.parentElement!==overview)grid.parentElement.appendChild(chat);$$('.assistant-card').forEach(b=>b.onclick=()=>openAssistant(b.dataset.assistant));const legacy=$('toolIzart');if(legacy){const card=legacy.closest('.tool-card');if(card)card.remove()}const toolCard=$('toolConvgpt')?.closest('.tool-card');if(toolCard&&!toolCard.querySelector('.provider-badges')){const wrap=document.createElement('div');wrap.className='provider-badges';PROVIDER_LABELS.forEach(p=>{const x=document.createElement('span');x.className='status-chip';x.textContent=p;wrap.appendChild(x)});toolCard.appendChild(wrap)}}
$('assistantClose').onclick=()=>{$('izartChat').hidden=true};$('assistantClear').onclick=()=>{$('izartMessages').replaceChildren();addMessage(currentAssistant,'Conversa limpa.')};$('assistantMin').onclick=()=>{$('izartChat').classList.toggle('chat-minimized')};$('izartSend').onclick=()=>{const i=$('izartInput'),v=i.value.trim();if(!v)return;addMessage('Você',v);setTimeout(()=>addMessage(currentAssistant,reply(currentAssistant,v)),120);i.value='';i.focus()};$('izartInput').addEventListener('keydown',e=>{if(e.key==='Enter')$('izartSend').click()});
async function updateConvDButton(){const s=await chrome.storage.local.get({convDEnabled:true}),on=s.convDEnabled!==false;$('convDStatus').textContent=on?'Ativo':'Desativado';$('toolConvgpt').textContent=on?'Desativar CONV':'Ativar CONV'}
$('toolConvgpt').onclick=async()=>{const s=await chrome.storage.local.get({convDEnabled:true}),on=!(s.convDEnabled!==false);await chrome.storage.local.set({convDEnabled:on});await updateConvDButton();$('hostResult').textContent=on?'CONV-D ativado. O botão Baixar Conversa aparecerá nas conversas suportadas.':'CONV-D desativado. O botão Baixar Conversa ficará oculto.'};
function supportedUrl(raw){try{const u=new URL(raw),h=u.hostname.toLowerCase();return /(^|\.)chatgpt\.com$|(^|\.)claude\.ai$|(^|\.)gemini\.google\.com$|(^|\.)copilot\.microsoft\.com$|(^|\.)perplexity\.ai$|(^|\.)grok\.com$|(^|\.)chat\.deepseek\.com$|(^|\.)poe\.com$|(^|\.)chat\.mistral\.ai$|(^|\.)you\.com$|(^|\.)meta\.ai$|(^|\.)chat\.qwen\.ai$|(^|\.)huggingface\.co$|(^|\.)character\.ai$/.test(h)?u:null}catch(_){return null}}
function addDownloadByLink(){if($('toolDownloadLink'))return;const grid=document.querySelector('.tool-grid');if(!grid)return;const card=document.createElement('article');card.className='tool-card';card.id='toolDownloadLink';card.innerHTML='<div class="tool-icon">🔗</div><div><h3>Download por Link</h3><p>Cole a URL de uma conversa suportada. O IZGITH abre exatamente essa conversa e o CONV-D aparece nela.</p></div><input id="convDUrl" class="tool-url" type="url" placeholder="https://.../conversa"><button id="convDOpenUrl" class="btn ghost">Abrir conversa</button>';grid.appendChild(card);$('convDOpenUrl').onclick=async()=>{const u=supportedUrl($('convDUrl').value.trim());if(!u){$('hostResult').textContent='URL inválida ou plataforma ainda não suportada pelo CONV-D.';return}await chrome.tabs.create({url:u.href,active:true});$('hostResult').textContent='Conversa aberta. Aguarde a página carregar; o botão Baixar Conversa será inserido automaticamente.'}}
$('toolSonpef').onclick=()=>$('sonpefFiles').click();$('sonpefFiles').onchange=async e=>{const files=[...e.target.files].filter(f=>/\.(ps1|py)$/i.test(f.name));if(!files.length){$('hostResult').textContent='Nenhum .ps1/.py selecionado.';return}const parts=[];for(const f of files)parts.push(`\n===== ${f.name} =====\n`,await f.text());const unified=`# SONPEF UNIFIED · IZGITH\n# Gerado no navegador · ${new Date().toISOString()}\n# Arquivos: ${files.length}\n${parts.join('')}\n`;const u=URL.createObjectURL(new Blob([unified],{type:'text/plain;charset=utf-8'})),a=document.createElement('a');a.href=u;a.download='sonpef_unified_izgith.ps1.txt';a.click();setTimeout(()=>URL.revokeObjectURL(u),1000);$('hostResult').textContent=`SONPEF concluído: ${files.length} arquivo(s) unificados.`};
$('toolKit').onclick=()=>{$('hostResult').textContent='KIT_UNICO: hub das integrações de assistentes. A conversa das IAs fica na área Assistentes da página inicial.'};$('hostPing').onclick=()=>{$('statHost').textContent='N/A';$('hostResult').textContent='Sistema autônomo verificado: Native Messaging não é requerido.'};$('clearQueue').onclick=async()=>{await chrome.storage.local.set({izgithQueue:[]});renderQueue()};$('randomTheme').onclick=()=>applyTheme(THEMES[Math.floor(Math.random()*THEMES.length)][0]);
$('minimizeDashboard').onclick=async()=>{const tabs=await chrome.tabs.query({active:true,currentWindow:true});const target=tabs.find(t=>t.id!==undefined&&!String(t.url||'').startsWith(chrome.runtime.getURL('')));if(target?.id!==undefined)await chrome.tabs.update(target.id,{active:true});const w=await chrome.windows.getCurrent();if(w.id!==undefined)await chrome.windows.remove(w.id)};$('closeDashboard').onclick=async()=>{const w=await chrome.windows.getCurrent();if(w.id!==undefined)await chrome.windows.remove(w.id)};
$('operationMode').onchange=e=>chrome.storage.local.set({operationMode:e.target.value});$('autoMode').onchange=e=>chrome.storage.local.set({autoMode:e.target.value});$('performanceMode').onchange=e=>{document.body.classList.toggle('reduce-motion',e.target.checked);chrome.storage.local.set({performanceMode:e.target.checked})};$('visualDepth').onchange=e=>{document.documentElement.dataset.depth=e.target.value;document.body.dataset.depth=e.target.value;chrome.storage.local.set({visualDepth:e.target.value})};
function enrichSettings(){const panel=$('settings')?.querySelector('.panel');if(!panel||panel.querySelector('.mode-guide'))return;const box=document.createElement('div');box.className='mode-guide';box.innerHTML='<h3>Como funcionam os modos</h3><p><b>Ultra + Controlado — Unificado:</b> combina automação rápida com salvaguardas. O IZGITH prepara tarefas e mantém etapas sensíveis sob controle, evitando execução externa silenciosa.</p><p><b>Controlado:</b> prioriza segurança e previsibilidade. Ações relevantes ficam preparadas e dependem da confirmação do usuário antes de avançar.</p><p><b>Ultra:</b> prioriza velocidade e automação dentro do que o navegador permite, reduzindo interrupções de confirmação.</p><h3>Como funciona o modo automático</h3><p><b>Auto preparar:</b> monta antecipadamente dados, arquivos, parâmetros e sequência de uma ação, mas não executa etapa externa nem envia algo fora do IZGITH por conta própria.</p><p><b>Auto com confirmação:</b> monta a ação e mostra o que será feito; o usuário confirma explicitamente antes da etapa que efetiva a operação.</p><p><b>Manual:</b> nada é preparado automaticamente. O usuário inicia cada etapa.</p>';panel.appendChild(box)}
(async()=>{buildThemeGrid();const s=await chrome.storage.local.get({theme:'cyber-01',operationMode:'unified',autoMode:'confirm',performanceMode:false,visualDepth:'3D',convDEnabled:true});applyTheme(s.theme);$('operationMode').value=s.operationMode;$('autoMode').value=s.autoMode;$('performanceMode').checked=!!s.performanceMode;$('visualDepth').value=s.visualDepth;document.documentElement.dataset.depth=s.visualDepth;document.body.dataset.depth=s.visualDepth;document.body.classList.toggle('reduce-motion',!!s.performanceMode);setupAssistantArea();addDownloadByLink();enrichSettings();await renderQueue();await updateConvDButton();openTab(['overview','tools','settings','queue','themes','eula','guide'].includes(location.hash.slice(1))?location.hash.slice(1):'overview');$('splash').classList.add('hidden')})();
