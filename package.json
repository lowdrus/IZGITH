{
  "name": "izgith",
  "version": "6.0.0-00039",
  "private": true,
  "description": "Extensão Manifest V3 para preparação e auditoria segura de extensões Chromium",
  "scripts": {
    "test": "python scripts/validate_project.py && python -m unittest discover -s tests -v",
    "package": "python scripts/package_extension.py"
  },
  "engines": {"node": ">=24"},
  "license": "SEE LICENSE IN LICENSE"
}
